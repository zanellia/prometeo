#ifndef {{ fun.name }}_H_
#define {{ fun.name }}_H_

int {{ fun.name }}(const double** arg, double** res, int* iw, double* w, int mem);

void {{ fun.name}}_eval(const double* in, double* out,
    int (*{{ fun.name}})(const double**, double**, int*, double*, int));

#endif  // {{ fun.name }}_H_
